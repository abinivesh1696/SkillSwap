# SkillSwap Hub

Frontend-only React demo app for skill exchange between students. Built with React, Vite, and Bootstrap 5.

Run:

```bash
npm install
npm run dev
```

Features:
- Explore student profiles
- Search and category filter
- Favorite profiles stored in localStorage
- Swap requests persisted in localStorage
- Dark theme, glassmorphism UI, responsive layout
